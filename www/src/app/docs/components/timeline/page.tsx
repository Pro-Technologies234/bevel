import { TimelineContent } from "@/components/docs/timeline";
// import { docsTimelineMetadata } from "@/lib/metadata";

// export const metadata = docsTimelineMetadata;

export default function TimelinePage() {
  return <TimelineContent />;
}
