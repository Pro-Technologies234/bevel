import { Installation } from "@/components/docs/installation";
export const metadata = {
  title: "Installation — Bevel UI",
  description:
    "Install Bevel UI systems via the shadcn CLI. Each system copies directly into your project — no npm package, no versioning, no lock-in.",
};

export default function InstallationPage() {
  return <Installation />;
}
